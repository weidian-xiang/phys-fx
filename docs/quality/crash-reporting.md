# 崩溃报告与隐私边界

当前桌面版本默认只写本地诊断日志，不自动上传。发布构建可由维护者启用平台 minidump
钩子，文件放在用户缓存目录并按启动次数轮换；首次开启上报必须显示隐私说明并取得用户
明确同意。上传内容只允许脱敏版本号、平台、信号和符号化堆栈，不包含视频、工程、路径、
API key 或模板内容。符号化流程：保存对应构建的 PDB/DWARF 到受控制品库，再使用
`llvm-symbolizer`/Visual Studio 调试器离线处理。

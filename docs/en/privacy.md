# Privacy

Telemetry and crash uploads are off by default. Local logs contain diagnostic status only. An
opt-in crash report may include a redacted version, platform, signal and symbolized stack, never
video, project contents, paths, API keys or template payloads. Cloud retention and deletion are
explicitly bounded; local rendering does not require network access.

# ADR-0015：Phase 5 CI 主平台

## 状态

已接受（Phase 5）。

## 决策

GitHub Actions 作为镜像仓的主质量验证平台，Gitee Go 保留为 Gitee 侧兼容预案。
`.github/workflows/ci.yml` 在 `master` push 和 PR 触发，覆盖默认构建、CTest、pytest、
版权和提交检查。Gitee 主仓仍是发布、PR、标签和 Release 的唯一来源；GitHub 不配置可用
push URL。切换结论基于镜像已建立、工作流文件已生效，首次真实运行记录由仓库 Actions
页面提供。

# ADR-0018：Phase 6 单机云服务技术栈

## 状态

已接受（参考实现）。

## 决策

先以 Python 标准库 `ThreadingHTTPServer`、SQLite 和本地对象目录兑现可验证的 API 契约；接口保留 FastAPI、Redis、MinIO 和容器化 CLI 的替换点。核心 C++ 不发起网络调用，云端入口只在 CLI/GUI 扩展层启用。

## 原因与边界

单机部署便于验证队列、配额、删除和失败诊断，不把运维复杂度提前扩展到多副本/k8s。公网服务必须补 HTTPS 反代、任务容器隔离、对象存储和水平扩展，再宣称商业可用。

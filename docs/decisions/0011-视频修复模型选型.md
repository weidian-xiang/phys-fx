# ADR-0011：视频修复模型选型

## 状态

调查中（Phase 4）。

## 决策方向

优先评估 ProPainter、E2FGVI 等可获得可靠 ONNX 导出的模型，要求 CPU 可推理、许可允许
再分发、支持时序传播并能在 720p 滑窗处理。模型权重不入库，来源、哈希和许可写入
`docs/model-cards.md`。

在模型锁定前使用 `InpaintingRenderer` CPU 基线；模型不可用时不静默宣称神经修复成功。

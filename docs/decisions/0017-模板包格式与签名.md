# ADR-0017：模板包格式与 Ed25519 签名

## 状态

已接受（Phase 5）。

## 决策

`.pfxtpl` 使用 ZIP 容器，包含 `manifest.json`、模板文件和可选 `SIGNATURE`。签名覆盖
规范化清单及按文件名排序的文件内容，算法为 Ed25519。`tools/pack_template.py` 调用
OpenSSL 完成离线签名和验签，私钥不得入库或进入 CI 明文。

签名仅表示来源信任，不是功能解锁：开源核心永远允许未签名包加载，验签失败显示中文
“未认证来源”标识但不阻断功能。

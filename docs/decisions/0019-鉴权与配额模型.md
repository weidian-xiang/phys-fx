# ADR-0019：鉴权、配额与隐私模型

## 状态

已接受（Phase 6 内测）。

## 决策

邮箱注册生成 API Key，服务端只存 SHA-256 哈希；渲染任务绑定用户并记录秒数/任务数。Phase 6 只计数不扣费，免费档默认 600 秒/月。任务记录默认 24 小时过期，删除接口可提前清理。

## 红线

本地渲染不需要 API Key；云服务失败不阻塞本地管线；遥测默认关闭；支付和企业版延后到 Phase 7。

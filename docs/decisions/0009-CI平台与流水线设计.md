# ADR-0009：CI 平台与流水线设计

## 状态

已接受（Phase 3）。

## 决策

Gitee Go 是主流水线，GitHub Actions 文件作为镜像预案。流水线先验证版权、提交
规范、默认零依赖构建、CTest 和 pytest，再用同一个依赖脚本获取经哈希锁定的 LGPL
FFmpeg shared 包与 ONNX Runtime，覆盖两个开关同时开启的构建。真实视频往返和粒子
遮挡 demo 作为 `needs_ffmpeg` 冒烟测试运行并保留输出制品；无模型时仅
`needs_models` 用例跳过。

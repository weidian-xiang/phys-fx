# ADR-0009：CI 平台与流水线设计

## 状态

已接受（配置占位）。

## 决策

Gitee Go 是主流水线，GitHub Actions 文件作为镜像预案。流水线先验证版权、提交
规范、默认零依赖构建、CTest 和 pytest，再在依赖缓存可用时覆盖 FFmpeg/ONNX 开关。
端到端视频冒烟测试在真实 IO 合入后追加；无模型时 `needs_models` 用例跳过。

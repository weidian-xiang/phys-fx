# Phase 3 性能基线

基线目标：720p、10 秒视频、CPU 掩码传播单帧低于 500 ms，全流程峰值内存低于
4 GB。当前仓库环境未安装合规的 FFmpeg C API 开发包和经许可锁定的 ONNX 权重，
因此不能给出真实模型数据，禁止用桩耗时冒充推理基线。

已测的无依赖路径（Windows MinGW Debug）：CPU 粒子与 8×8 遮挡像素测试通过，
10k 粒子上限已在配置契约中固定。待模型与 LGPL 动态 FFmpeg 就位后记录机器型号、
输入分辨率、帧数、每阶段 P50/P95 耗时和进程峰值内存，并以真实数据替换本段。

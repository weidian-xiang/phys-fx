[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "physfx-py"
version = "0.5.0"
description = "PhysFX Engine Python API"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "physfx-py"
version = "0.2.0.dev0"
description = "PhysFX Engine Python API Phase 1 骨架"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]

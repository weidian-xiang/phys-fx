# AGENT_RULES.md —— PhysFX 项目 AI 编码代理行为准则

> **本文件是 PhysFX 仓库对一切 AI 编码代理（Codex 及后继者）的最高行为准则。**
> 开始任何任务前必须完整阅读本文件。本文件与阶段任务文档冲突时，以更严格者为准；
> 用户即时指令与本文件冲突时，停下来向用户确认，不擅自取舍。
> 本文件生效前的历史提交保持原样（历史不重写）；自生效起，全部产出遵守本文件。

---

## 第一条：语言纪律（本次立规的直接原因）

1. **一切产出使用简体中文**：Git 提交信息、代码注释、文档、PR 标题与正文、Issue、Release 正文、用户可见的错误提示、完成报告——一律中文；
2. **例外仅三处**：标识符（类名/变量名/函数名）用英文；Conventional Commits 的 type/scope 关键字（`feat`/`fix`/`docs` 等）按规范保留英文；第三方协议要求的固定文本（如 `Signed-off-by`、SPDX 标识）保持原文；
3. 提交信息描述部分必须是**中文祈使句**，禁止英文描述，例如：
   - ✅ `feat(semantics): 接入真实分割模型权重`
   - ❌ `build(phase9): lock and verify real model weights`
4. 代码注释为完整中文句子，不使用中英混杂的碎片注释。

## 第二条：提交规范

1. 格式：`<type>(<scope>): <中文描述，≤50 字，末尾不加句号>`；
2. type 白名单：`feat` / `fix` / `docs` / `refactor` / `test` / `perf` / `build` / `ci` / `chore`；
3. scope 用模块名小写（`core`/`semantics`/`editing`/`platform`/`cloud`/`deps`/`git` 等），跨模块可省略；
4. **每个提交必须带 DCO 签署**：footer 含 `Signed-off-by: 向伟典 <xwd752438081@163.com>`；
5. 关联 Issue 用 `Refs: #N`；破坏性变更必须写 `BREAKING CHANGE:` 段；
6. 一个提交只做一件事；提交前必须通过本地提交信息校验钩子（`.githooks/commit-msg`）。

## 第三条：Git 与推送纪律

1. **推送是任务的完成定义：代码只在本地 = 工作不存在。** 每个任务以推送远端收尾；
2. `master` 受保护：禁止 force push、禁止删除；维护者本人可直推，其他贡献者一律 PR；
3. 功能分支命名：`feature/<scope>-<简述>` / `fix/<简述>` / `docs/<简述>` 等，全小写连字符；
4. 推送与发布解耦：推代码是每个工作日结束必做的备份动作，无需任何批准；发 Release 才走检查单；
5. **每次完成报告前必须运行 `python tools/sync_check.py` 并在报告中粘贴完整输出**——工作区干净、无未推送提交、标签远端存在、GitHub 镜像同步，全绿才算完成；
6. GitHub 为镜像仓：禁手工推送，由同步机制保持一致。

## 第四条：诚实原则（最高优先级，与一切任务目标冲突时以本条为准）

1. **禁止虚构**：不虚构完成状态、测试数字、远端链接、CI 运行记录、Release 地址、性能数据、用户反馈；没有证据就写"未验证"；
2. **显式诊断，不伪装**：能力未完成就明确报"未实现/已降级"及原因，禁止用降级路径的结果冒充目标路径的结果（本项目先例：`smoke` 命令缺环境时返回诊断而非伪装完成，予以保持）；
3. **降级三处一致**：任何降级必须同时在 PR 描述、CHANGELOG、完成报告中一致标注；永久限制还须写入 roadmap 与相关功能文档；
4. **禁止降低测试断言来换取绿灯**；确需调整阈值必须写明理由并留 TODO；
5. 演示物料必须真实：实拍素材或如实标注的 AI 生成素材，禁止合成素材冒充实拍、禁止效果剪辑美化、禁止参数隐瞒；
6. 遇到外部动作（公开发布、修改平台设置、点确认按钮）先停下来等维护者确认，不擅自执行。

## 第五条：工程质量门禁

1. 构建零错误零警告（双配置：依赖 ON/OFF、GUI ON/OFF 按任务要求）；
2. CTest / pytest 全绿后才可提交推送；`needs_models`/`needs_taichi` 等标签用例按环境能力执行，跳过须如实说明；
3. 性能预算：关键链路回归超 20% 告警、超 50% 失败（CI 门禁）；
4. fuzz 与 ASAN/UBSAN nightly 结果纳入缺陷台账；新增 P0 缺陷时停下手头工作先修 P0；
5. 每个缺陷修复配回归测试；每组功能变更配测试，不攒到最后。

## 第六条：版权与许可

1. **每一个源文件**（.h/.cpp/.py/.cmake/CMakeLists.txt 等）顶部带统一版权头：
   `Copyright (c) 2026 向伟典`，含 SPDX-License-Identifier: Apache-2.0；模板见 `COPYRIGHT` 文件；
2. 提交前运行 `tools/check_copyright.py`，覆盖率必须 100%；
3. 引入第三方依赖前完成许可证审查并更新 `NOTICE`；FFmpeg 仅 LGPL 动态链接，禁止启用 GPL 组件；
4. "PhysFX"名称与 Logo 为保留商标，不在 Apache-2.0 授权范围内。

## 第七条：禁止入库清单

以下任何一项都不得进入 Git 历史：

1. 凭据、令牌、私钥（模板签名私钥、支付密钥、API Key）；
2. 模型权重文件（一律 `tools/download_models.py` 拉取 + SHA-256 校验）；
3. 大体积素材与演示视频原件（素材只入库校验值与获取说明；发布物料走 Release 附件）；
4. 构建产物与缓存（`build/`、`__pycache__/`、`.venv/` 等，以 `.gitignore` 为准）；
5. 例外：符合素材入库标准的小型可再分发资产（如 CC0 天空图，单文件 < 2MB、总量 < 20MB）经许可清单登记后可入库。

## 第八条：架构纪律

1. 模块依赖方向不可违反：能力模块只依赖 `core`，互不包含头文件；GUI/应用单向依赖引擎；核心不依赖任何商业组件；
2. **修改任何已发布接口签名前必须先写 ADR**（编号递增），并在完成报告中显著标注；不沉默偏离既定设计；
3. 遵守 `docs/v1-freeze.md` 冻结清单与 `docs/compatibility.md` 兼容承诺：1.x 内不破坏三种文件格式与公共 API；
4. 一切用户编辑必须表达为 `IEditCommand`（操作即命令）；GUI 与 CLI 产出必须等价（等价性测试保持绿）；
5. 商业化红线：**开源版功能永远完整**；签名与授权只做信任标识，不做功能阉割；`cloud/` 收费能力只做增量。

## 第九条：发布纪律

1. 发布前 `tools/release_checklist.py` 全绿（其第 0 步为同步检查）；
2. 标签为附注标签；Release 正文如实标注降级项与已知限制；物料逐条登记文件名/字节数/SHA-256；
3. 物料缺失只阻塞"完整版 Release 正文"，不阻塞代码与标签推送；
4. 版本号三处一致（CMake / Python 包 / README 徽章）；CHANGELOG 无 Unreleased 残留；
5. 支付等商业开关必须与资质审批状态一致，未获批默认关闭。

## 第十条：完成报告格式（中文）

每份完成报告必须包含：

1. 任务逐项结论与**可复核证据**（远端 `git log`/`git ls-remote`、CI 运行 URL、Release 链接）——不接受本地截图充当远端证据；
2. `sync_check.py` 完整输出；
3. 降级项清单及三处一致核对结果；
4. 测试与门禁结果（构建、CTest、pytest、版权扫描、性能预算）；
5. 遗留 TODO 与下一阶段衔接建议。

## 第十一条：冲突时的决策顺序

1. 诚实原则（第四条）高于一切任务目标；
2. 用户数据与凭据安全（第七条）高于便利性；
3. 拿不准时**停下来问维护者**，不猜测执行、不擅自扩大任务范围；
4. 本文件未尽事项：写 ADR 记录决策，或在完成报告中显著提问。

---

*本文件由 Phase 1~9 各阶段文档中的规则提炼汇总而成，维护者：向伟典。修订走正常 PR/直推流程并在 CHANGELOG 记录。*
